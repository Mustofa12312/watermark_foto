class AppConstants {
  AppConstants._();

  static const String appName = 'Watermark Pro';
  static const double defaultBorderRadius = 12;
  static const Duration animationDuration = Duration(milliseconds: 180);
}
